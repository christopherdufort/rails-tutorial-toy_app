<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<!--If one existis show the contents of the user's first micropost-->
<% unless (@user.microposts.first.nil?) %> 
  <p>
    <strong>First Micropost:</strong>
  <%= @user.microposts.first.content %>
  </p>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
